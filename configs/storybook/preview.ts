import "../../src/style.css";

const preview = {
  parameters: {
    backgrounds: {
      default: "app",
      values: [
        { name: "app", value: "#f6f7fb" },
        { name: "dark", value: "#12141c" },
      ],
    },
  },
};

export default preview;
